---
const { title, summary, image, link } = Astro.props;
---
<article class="blog-card">
  <img src={image} alt={title} />
  <div class="blog-card-content">
    <h2>{title}</h2>
    <p>{summary}</p>
    <a href={link}>Read More →</a>
  </div>
</article>
